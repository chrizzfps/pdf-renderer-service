{
  "name": "fusion-pdf-renderer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "install:browser": "playwright install chromium",
    "render:install": "playwright install chromium"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "playwright": "^1.61.1"
  }
}
